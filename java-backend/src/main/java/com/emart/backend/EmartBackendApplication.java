package com.emart.backend;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main Spring Boot application class for EMart backend services
 * This is a standalone backend for GitHub language statistics
 */
@SpringBootApplication
public class EmartBackendApplication {

    public static void main(String[] args) {
        SpringApplication.run(EmartBackendApplication.class, args);
    }
} 